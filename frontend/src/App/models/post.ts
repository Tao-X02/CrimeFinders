// Interface to define a model for post
export interface Post {
    id: string;
    type: string;
    description: string;
    city: number;
    region: string;
    location: string;
    date: string;
}